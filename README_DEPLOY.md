# Plankton - AI Plant Assistant

Aplikasi asisten tanaman berbasis AI menggunakan Flask, Plant.id API, dan Groq AI.

## Features
- 🌿 Plant identification menggunakan Plant.id API
- 🤖 AI chatbot untuk konsultasi tanaman
- 📊 Health assessment & disease detection
- 📜 Chat history & analysis history
- 👤 User authentication

## Tech Stack
- **Backend**: Flask, Flask-SQLAlchemy, Flask-Login
- **APIs**: Plant.id, Groq AI
- **Database**: SQLite
- **Frontend**: HTML, CSS, Vanilla JavaScript

## Local Development

### Prerequisites
- Python 3.8+
- Virtual environment
- API Keys (Plant.id & Groq)

### Setup
```bash
# Clone repository
git clone <your-repo-url>
cd plankton

# Create virtual environment
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# venv\Scripts\activate  # Windows

# Install dependencies
pip install -r requirements.txt

# Setup environment variables
cp .env.example .env
# Edit .env with your API keys

# Run application
python run.py
```

### Environment Variables
```
FLASK_ENV=development
SECRET_KEY=your-secret-key
PLANTID_API_KEY=your-plantid-api-key
GROQ_API_KEY=your-groq-api-key
DATABASE_URL=sqlite:///plankton.db
```

## Deployment (Render)

### Auto Deploy
1. Push to GitHub
2. Connect repository to Render
3. Render will auto-detect `render.yaml`
4. Add environment variables in Render dashboard
5. Deploy!

### Manual Deploy
1. Create new Web Service on Render
2. Connect GitHub repository
3. Build Command: `./build.sh`
4. Start Command: `gunicorn run:app`
5. Add environment variables
6. Deploy

### Required Environment Variables on Render
- `FLASK_ENV=production`
- `SECRET_KEY` (auto-generated by Render)
- `PLANTID_API_KEY`
- `GROQ_API_KEY`
- `DATABASE_URL=sqlite:///plankton.db`

## API Keys
- **Plant.id**: https://plant.id
- **Groq**: https://console.groq.com

## Project Structure
```
plankton/
├── app/
│   ├── __init__.py
│   ├── models.py
│   ├── routes/
│   │   ├── auth.py
│   │   ├── chat.py
│   │   ├── main.py
│   │   └── plant_analysis.py
│   ├── services/
│   │   ├── groq_service.py
│   │   └── plantid_service.py
│   ├── static/
│   │   ├── css/
│   │   ├── js/
│   │   └── uploads/
│   └── templates/
├── run.py
├── requirements.txt
├── Procfile
├── build.sh
└── render.yaml
```

## License
MIT License
